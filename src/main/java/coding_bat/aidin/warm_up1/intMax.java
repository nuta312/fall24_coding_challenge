package coding_bat.aidin.warm_up1;

public class intMax {
    public int intMax(int a, int b, int c) {
        int MAX = a;

        if ( b > MAX) MAX = b;

        if ( c >MAX) MAX = c;

        return MAX;

    }
}
