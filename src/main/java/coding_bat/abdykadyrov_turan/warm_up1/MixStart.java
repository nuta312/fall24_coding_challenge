package coding_bat.abdykadyrov_turan.warm_up1;

public class MixStart {
    public boolean mixStart(String str) {
        if (str.startsWith("ix", 1) || str.startsWith("ix", 0)) {
            return true;
        }else {
            return false;
        }
    }
}
