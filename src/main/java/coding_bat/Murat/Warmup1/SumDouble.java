package coding_bat.Murat.Warmup1;

public class SumDouble {
    public int sumDouble(int a, int b) {
        if (a != b) return a + b;
        else if (a == b) return (a + b) * 2;
        return 0;
    }
}
